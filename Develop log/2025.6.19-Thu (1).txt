今日目标：实现所有基础功能，能正常走棋，胜负判定，集成ui页面，生成1.0版本

共同讨论：讨论分工情况，未来开发计划

————————————————————————————————————————————————————————————————————————
分工情况：

项载顺：
设计方案：
吃过路兵：
    添加一个坐标属性标记所有棋子上一次所在的坐标，添加一个指针标识上一次移动的棋子。再添加一个属性（致命点）给棋子，专门给小兵设计的：初始为空，一旦小兵一次性移动两个格子，则致命点记录小兵上次位置和当前位置的中心点位。
    小兵移动时:
    if(检测相邻的两个格子（x+1/x-1）有其他阵营的小兵):
        if（被检测到的小兵是上一次移动的棋子，并且此时坐标和上次坐标差的绝对值=2）
            将该小兵的前进方向的后面一个坐标添加到当前要移动小兵的可选路径
    移动时：
    if(移动的棋子是小兵)
        在更新lastPiece之前:if(lastPiece棋子的致命点位置坐标和当前棋子的落点坐标重合)
            调用捕获函数将lastPiece指向的棋子销毁
将军：
    棋子初始化时将单个阵营的棋子的可移动路径全部放在数组：单个是Qlist（Qlist（QPoint））这样的格式，中间的Qlist记录一个棋子的所有可移动坐标）//局部对象，用完判断一次就销毁，因为每次走棋攻击范围都会变
    同时在初始化时单独为两个王添加两个指针到chessBoard中
        每次棋子移动，更新该阵营的所有棋子在对应阵营的可移动路径
            if(敌方阵营王的当前位置被包含)
                将敌方阵营的王所处的坐标各自标红
设计主菜单UI图，连接页面UI图
解决昨天遗留问题，实现正常吃子
设计和实现基础走棋规则，胜负判定


邓：
葛：完成菜单“连接”这个按钮点击时候的操作，弹出对应的菜单栏，然后就是两个菜单的连接，主菜单和连接这个菜单，实现计时操作，白方棋子的计时，总时间的计时，以及计时结束后那方获胜的情况

————————————————————————————————————————————————————————————————————————
总结：目前未完成集成，其他均已实现
