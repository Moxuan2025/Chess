今日目标：继续集成，解决昨日遗留问题，并且修复部分bug

——————————————————————————————————————————————————————————
分工情况：

项载顺
设计方案：
悔棋：
    给棋盘对象添加一个数组/容器，每次棋子移动完毕将lastPiece和他上次坐标（lastposition）添加到这个数组/容器中
    并设置一个指针指向最后添加到元素
    每当用户执行悔棋操作（仅在对方轮次可操作）则将从指针对应的棋子移动到保存的点位，然后在数组移除该元素，之将指针往上一个元素，当数组大小为空时，禁止悔棋操作
    并且悔棋操作进行时，若是兵则将致命点设置为空
    后续优化：不设置数组，用QStack栈对象保存，每一个元素都是结构体

实现悔棋功能，优化对战UI页面

邓龙溪：
集成qml文件，将几个页面连接起来（主菜单，连接页面，对战页面），优化UI页面

葛防：
增加计时器选择类型，增加落子音效，优化UI页面

———————————————————————————————————————————————————————————————
总结：今日目标大体达成，对战返回主菜单栏没有实现，悔棋功能存在部分bug（不能恢复被吃的“过路兵”）
