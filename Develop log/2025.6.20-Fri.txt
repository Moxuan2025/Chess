设计方案：
悔棋：
    给棋盘对象添加一个数组/容器，每次棋子移动完毕将lastPiece和他上次坐标（lastposition）添加到这个数组/容器中
    并设置一个指针指向最后添加到元素
    每当用户执行悔棋操作（仅在对方轮次可操作）则将从指针对应的棋子移动到保存的点位，然后在数组移除该元素，之将指针往上一个元素，当数组大小为空时，禁止悔棋操作
    并且悔棋操作进行时，若是兵则将致命点设置为空
